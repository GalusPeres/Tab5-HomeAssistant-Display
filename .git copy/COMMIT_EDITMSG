Fix: Power Management stabilisiert

Änderungen:
- CPU_FREQ_SLEEP auf 80 MHz erhöht (40 MHz → DMA Underrun + türkises Blitzen)
- FPS_SLEEP bleibt bei 1 FPS (funktioniert stabil)
- IDLE_TIMEOUT_MS bei 3 Sekunden (bewährter Wert)
- Config-Fix: auto_sleep_minutes = 0 wird auf 1 korrigiert
- WiFi Power Saving basierend auf Batterie/Netzteil aktiv
- Stromrichtungs-Detection (batCurrent <= 50 = Netzteil)
- Wakeup-Sequenz: CPU → Display → LVGL (10 FPS) → 60 FPS

Technische Details:
- MIPI-DSI Display braucht min. 80 MHz CPU für DMA-Operationen
- Bei 40 MHz: DMA Underrun + Framebuffer-Probleme (türkis)
- Stromersparnis hauptsächlich durch: Display aus, WiFi Power Saving, LVGL 1 FPS

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
